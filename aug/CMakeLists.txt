cmake_minimum_required(VERSION 2.8)
project( aug )
find_package( OpenCV REQUIRED )
include_directories( include ${OpenCV_INCLUDE_DIRS} )
file( GLOB SRCS src/*)
add_executable( aug ${SRCS} )
target_link_libraries( aug ${OpenCV_LIBS} )