cmake_minimum_required(VERSION 2.8)
project( task3 )
find_package( OpenCV REQUIRED )
include_directories( include ${OpenCV_INCLUDE_DIRS} )
file( GLOB SRCS src/*.cpp)
add_executable( task3 ${SRCS} )
target_link_libraries( task3 ${OpenCV_LIBS} )