#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>
#include <algorithm>
#include <opencv2/opencv.hpp>

void readFiles(std::string path);

std::vector<int> perm(int size);

#endif
